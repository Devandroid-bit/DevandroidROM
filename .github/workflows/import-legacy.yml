name: Import legacy

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: import-legacy
  cancel-in-progress: false

jobs:
  import-targets:
    name: Import legacy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout sixteen branch
        uses: actions/checkout@v4
        with:
          ref: sixteen
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Clone source repository
        run: |
          git clone \
            --depth=1 \
            --branch sixteen \
            https://github.com/salvogiangri/UN1CA.git \
            source-repo

      - name: Import prebuilts
        run: |
          set -e

          SOURCE_DIR="source-repo/unica/patches/legacy"
          DEST_DIR="unica/patches/legacy"

          if [ ! -d "$SOURCE_DIR" ]; then
            echo "::error::Source directory does not exist: $SOURCE_DIR"
            exit 1
          fi

          echo "Removing existing unica/patches/legacy..."
          rm -rf "$DEST_DIR"

          echo "Creating destination directory..."
          mkdir -p "$DEST_DIR"

          echo "Copying unica/patches/legacy..."
          rsync -a --delete "$SOURCE_DIR/" "$DEST_DIR/"

          echo "unica/patches/legacy..."

      - name: Check imported files
        run: |
          echo "Target files:"
          find target -type f | wc -l

          echo "unica/patches/legacy..."
          find unica/patches/legacy -type f | wc -l

      - name: Check for changes
        id: changes
        run: |
          git add -A unica/patches/legacy

          if git diff --cached --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No changes detected."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Changes detected."
          fi

      - name: Commit changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          git commit \
            -m "unica: patches: legacy: init" \
               -m "Author and credits: @salvogiangri"

      - name: Rebase sixteen
        if: steps.changes.outputs.changed == 'true'
        run: |
          git fetch origin sixteen

          git rebase origin/sixteen

      - name: Push changes to sixteen
        if: steps.changes.outputs.changed == 'true'
        run: |
          git push --force-with-lease origin HEAD:sixteen

      - name: Cleanup
        if: always()
        run: |
          rm -rf source-repo
